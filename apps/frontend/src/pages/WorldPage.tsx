import { WorldCanvas } from "../components/world/WorldCanvas";

/**
 * Explorable third-person office world — premium interior + courtyard,
 * walked at eye level. All UI/canvas wiring lives in WorldCanvas.
 */
export function WorldPage() {
  return <WorldCanvas />;
}

export default WorldPage;
